<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.test1">

    <application
        android:allowBackup="true"
        android:icon="@drawable/th"
        android:label="@string/app_name"
        android:roundIcon="@drawable/th"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".page.init.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".page.teacher.teacher" />
        <!--<activity android:name=".page.init.ads" />-->
        <activity android:name=".page.student.student" />
        <activity android:name=".page.init.register" />
        <activity android:name=".page.student.Yangxiaohan" />
        <activity android:name=".page.teacher.issus_publication" />
        <activity android:name=".page.student.ApplyTopic" />
        <activity android:name=".page.teacher.AuditApply" />
        <activity android:name=".page.student.SubmitAchieve" />
        <activity android:name=".page.teacher.ExamineAchievement"></activity>
    </application>

</manifest>